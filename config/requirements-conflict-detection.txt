# Conflict Detection Tools Requirements
# References root requirements.txt for version consistency
# Add only conflict detection specific dependencies here

# Reference root requirements for version consistency
-r ../requirements.txt

# =============================================================================
# CONFLICT DETECTION SPECIFIC DEPENDENCIES (if any)
# =============================================================================

# Add any conflict detection specific dependencies here
# These should be truly additive and not conflict with root requirements
