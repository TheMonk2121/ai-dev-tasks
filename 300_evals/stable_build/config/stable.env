# Stable evaluation configuration
# Conservative settings for reliable baseline runs

# Few-shot settings (disabled by default)
FEW_SHOT_K=0
FEW_SHOT_SELECTOR=none
FEW_SHOT_SEED=42

# Chain of thought (disabled for stability)
EVAL_COT=0

# Retrieval settings
RETRIEVAL_LIMIT=450
RERANK_LIMIT=10
CHUNK_OVERLAP=0.8
CHUNK_PREFIX=A

# Rate limiting (conservative)
RATE_LIMIT_PROFILE=stable
MAX_WORKERS=3
