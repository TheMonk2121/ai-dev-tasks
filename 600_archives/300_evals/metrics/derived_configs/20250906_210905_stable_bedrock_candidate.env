# Derived from stable_bedrock.env @ 20250906_210905
# LESSONS_APPLIED=LL-2025-09-07-9e09,LL-2025-09-06-002

USE_BEDROCK_QUEUE=1
ASYNC_MAX_CONCURRENCY=1                    # Single async operation at a time
BEDROCK_MAX_CONCURRENCY=1                  # Single concurrent request
BEDROCK_MAX_RPS=0.15                       # Very conservative rate
BEDROCK_COOLDOWN_SEC=30                    # Long cooldown after 429s
BEDROCK_RETRY_MAX=8                        # More retries
BEDROCK_RETRY_BASE=1.8                     # Exponential backoff
BEDROCK_RETRY_MAX_SLEEP=20                 # Max sleep time
RAGCHECKER_COVERAGE_REWRITE=0              # Disable coverage rewrite
RAGCHECKER_JSON_PROMPTS=0                  # Disable JSON prompts
RAGCHECKER_JSON_MAX_TOKENS=200
RAGCHECKER_EVIDENCE_JACCARD=0.07
RAGCHECKER_EVIDENCE_COVERAGE=0.20
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
NEW_TEST_PARAM=test_value