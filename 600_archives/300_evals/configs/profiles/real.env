# Real evals (baseline/tuning)
# Derived by loader: EVAL_DRIVER=dspy_rag, RAGCHECKER_USE_REAL_RAG=1
# Set only concrete infra values here; secrets may come from CI env.
POSTGRES_DSN="postgresql://danieljacobs@localhost:5432/ai_agency"
DATABASE_URL="postgresql://danieljacobs@localhost:5432/ai_agency"
EVAL_PROFILE="real"
EVAL_DRIVER="dspy_rag"
RAGCHECKER_USE_REAL_RAG="1"
RETRIEVER_MIN_CHARS="140"
RETRIEVER_MIN_CHARS_SHORT_QUERY="80"
