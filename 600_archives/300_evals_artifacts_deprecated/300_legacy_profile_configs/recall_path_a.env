# Path A: Retrieval breadth + smarter fusion (highest ROI)
# Goal: raise the chance gold appears in candidates, then keep it near the top for the reader

# Larger candidate set + fusion
export RETR_TOPK_VEC=140
export RETR_TOPK_BM25=140
export RETR_TOPK_OTHER=60
export FUSION_METHOD=RRF
export RRF_K=60

# Cross-encoder reranker on fused set
export RERANK_ENABLE=1
export RERANK_MODEL=bge-reranker-large
export RERANK_TOPN=18

# Limited concurrency so Bedrock stays happy
export PIPELINE_WORKERS=2
export BEDROCK_MAX_IN_FLIGHT=1

# Evidence budget & filter logic (Path C light)
export EVIDENCE_SENT_BUDGET=12
export EVIDENCE_PER_DOC_MAX=3
export EVIDENCE_PER_SECTION_MAX=2
export J_MIN=0.05
export COVERAGE_MIN=0.15
export NOVELTY_FIRST=1
export DEDUP_NGRAM=5

# Conservative Bedrock settings
export BEDROCK_MAX_RPS=0.12
export BEDROCK_CALL_TIMEOUT_SEC=35
export BEDROCK_TEXT_TIMEOUT_SEC=25
export RAGCHECKER_BYPASS_CLI=1
export RAGCHECKER_DISABLE_EMBEDDINGS=1
