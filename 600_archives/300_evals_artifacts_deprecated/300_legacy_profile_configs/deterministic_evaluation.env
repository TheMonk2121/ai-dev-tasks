# Deterministic Evaluation Configuration
# Ensures reproducible evaluation results with temperature=0 and cache disabled

# Core Deterministic Settings
export TEMPERATURE=0.0
export EVAL_DISABLE_CACHE=1
export PROMPT_AUDIT=1
export RANDOM_SEED=42

# Prompt Audit Settings
export FEW_SHOT_IDS=""
export PROMPT_HASH=""
export COT_FLAG=0

# Cache and Reproducibility
export DISABLE_RETRIEVAL_CACHE=1
export DISABLE_EMBEDDING_CACHE=1
export DISABLE_RERANK_CACHE=1

# Evaluation Determinism
export EVAL_DETERMINISTIC=1
export EVAL_REPRODUCIBLE=1
export EVAL_AUDIT_TRAIL=1

# Model Determinism
export BEDROCK_TEMPERATURE=0.0
export BEDROCK_TOP_P=1.0
export BEDROCK_TOP_K=1
export BEDROCK_MAX_TOKENS=1000

# RAG System Determinism
export RETRIEVAL_DETERMINISTIC=1
export RERANK_DETERMINISTIC=1
export FUSION_DETERMINISTIC=1

# Logging and Audit
export EVAL_AUDIT_LOG=1
export PROMPT_AUDIT_LOG=1
export RETRIEVAL_AUDIT_LOG=1
export GENERATION_AUDIT_LOG=1

# Performance Settings (Conservative)
export PIPELINE_WORKERS=1
export BEDROCK_MAX_IN_FLIGHT=1
export BEDROCK_MAX_RPS=0.1
export BEDROCK_CALL_TIMEOUT_SEC=45
export BEDROCK_TEXT_TIMEOUT_SEC=30

# Quality Assurance
export EVAL_QUALITY_GATES=1
export EVAL_VALIDATION=1
export EVAL_CHECKSUMS=1

# Audit Trail
export EVAL_MANIFEST=1
export EVAL_TRACE=1
export EVAL_PROVENANCE=1
