# Reranker Disabled Configuration
# Source this file to disable the PyTorch reranker

# Disable PyTorch reranker
export RERANKER_ENABLED=false

# Disable reranking in the pipeline
export RERANK_ENABLE=0

# Evaluation settings
export EVAL_DISABLE_CACHE=1  # Disable cache for evaluations
