# AI Dev Tasks - Generic Environment Settings Only
# Project-specific RAGChecker/Bedrock configs should be loaded via:
# - source throttle_free_eval.sh (for ultra-conservative runs)
# - source configs/stable_bedrock.env (for stable baseline runs)

# Generic PATH and editor settings only
# (No BEDROCK_* or RAGCHECKER_* exports here to avoid conflicts)

if [[ $- == *i* ]]; then echo "✅ AI Dev Tasks generic environment loaded"; fi
