# AI Dev Tasks - Persistent Environment Configuration
# This file is automatically loaded by shell integration

# =============================================================================
# DATABASE CONFIGURATION (PERSISTENT)
# =============================================================================

# PostgreSQL Database - Set once, use everywhere
export POSTGRES_DSN="postgresql://danieljacobs@localhost:5432/ai_agency"
export DATABASE_URL="postgresql://danieljacobs@localhost:5432/ai_agency"

# =============================================================================
# EVALUATION CONFIGURATION (PERSISTENT)
# =============================================================================

# DSPy RAG System Path
export DSPY_RAG_PATH="src"

# Evaluation Driver (dspy_rag for real evaluations)
export EVAL_DRIVER="dspy_rag"

# RAGChecker Configuration
export RAGCHECKER_USE_REAL_RAG="1"

# Reranker Configuration
export RERANKER_MODEL="BAAI/bge-reranker-v2-m3"
export RERANK_ENABLE="1"

# Retrieval Configuration
export RETR_TOPK_VEC="50"
export RETR_TOPK_BM25="50"

# Reranker Configuration
export RERANK_ENABLE="1"
export RERANK_MODEL="BAAI/bge-reranker-base"

# =============================================================================
# PROJECT CONFIGURATION (PERSISTENT)
# =============================================================================

# UV Environment
export UV_PROJECT_ENVIRONMENT=".venv"

# Evaluation concurrency
export EVAL_CONCURRENCY="3"

# =============================================================================
# SHELL INTEGRATION
# =============================================================================

# Suppress bash deprecation warning on macOS
export BASH_SILENCE_DEPRECATION_WARNING=1

# Load confirmation
if [[ $- == *i* ]]; then 
    echo "✅ AI Dev Tasks persistent environment loaded"
    echo "   📊 Database: ${POSTGRES_DSN:0:30}..."
    echo "   🔧 Evaluation: $EVAL_DRIVER"
    echo "   💡 To load specific profiles: source scripts/shell/setup/load_profile.sh [gold|real|mock]"
fi
