# Cursor Rules (Entrypoint)

## 🚨 START EVERY CHAT WITH THIS

**At the beginning of every new chat, automatically run this command to prime the model with core documentation:**

```bash
python3 scripts/prime_cursor_chat.py planner "current project status and core documentation"
```

**Then copy the entire bundle output into the chat as the first message.**

## Core Rules

- **Formatting**: No bracketed placeholders in Markdown; prefer backticks/quotes
- **PRD Linkage**: Always include backlog item reference and run validator: `python3 scripts/validate_prd_backlog_linkage.py`
- **Memory**: Use database-based rehydration before reading files directly

## Modular Rules

See `.cursor/rules/**` for detailed rules:
- `.cursor/rules/onboarding.cursorrules` - Memory rehydration and session startup
- `.cursor/rules/formatting.cursorrules` - Markdown and documentation standards
- `.cursor/rules/prd-linkage.cursorrules` - PRD-backlog linkage requirements
- `.cursor/rules/workflows.cursorrules` - Development workflows and processes
- `.cursor/rules/code-compliance.cursorrules` - Code quality and writing standards

## Quick Reference

- **Start**: `python3 scripts/prime_cursor_chat.py planner "current project status and core documentation"`
- **Validate**: `python3 scripts/validate_prd_backlog_linkage.py`
- **Memory**: `python scripts/update_cursor_memory.py`
- **Dashboard**: `./dspy-rag-system/start_mission_dashboard.sh`
