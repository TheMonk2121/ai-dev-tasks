# DSPy RAG System Requirements
# References root requirements.txt for version consistency
# Add only subproject-specific dependencies here

# Reference root requirements for version consistency
-r ../requirements.txt

# =============================================================================
# SUBPROJECT-SPECIFIC DEPENDENCIES (if any)
# =============================================================================

# Add any DSPy RAG system specific dependencies here
# These should be truly additive and not conflict with root requirements

# NiceGUI for interactive graph visualization
nicegui>=1.4.0
httpx>=0.24.0

# =============================================================================
# OPTIONAL OFFICE DOCUMENT PROCESSING DEPENDENCIES
# =============================================================================
# These are optional dependencies for the Office MCP server
# Install only if you need to process Word, Excel, or PowerPoint documents
python-docx>=0.8.11      # For Word document processing
openpyxl>=3.1.0          # For Excel spreadsheet processing
python-pptx>=0.6.21      # For PowerPoint presentation processing

# =============================================================================
# OPTIONAL HTML PROCESSING DEPENDENCIES
# =============================================================================
# These are optional dependencies for HTML content processing
# Install only if you need to process HTML documents
beautifulsoup4>=4.12.0   # For HTML parsing and text extraction
