
# **Updated Schema Instructions (Version 21.0)**

## 📌 **Overview**
This updated schema framework enhances **data lineage tracking, schema validation, processing scalability, and structured BI output compatibility**. These updates ensure **seamless integration between structured datasets, fact/dimension tables, and analytical workflows.**

---

## **🔹 Key Enhancements in Version 21.0**
OK **Multi-Line Header Merging with Variable-Length Row Handling**  
OK **Pre-Fuzzy Alias Mapping to Improve Performance & Accuracy**  
OK **Expanded Logging (Borderline Matches & Summarized Schema Violations)**  
OK **Centralized & Incremental Logging for Easier Querying**  
OK **Optimized Referential Integrity & Type Enforcement**  
OK **Manual Override for File-Specific Aliases Before Global Dictionary**  
OK **Expanded Synthetic Test Coverage for CI/CD Pipelines**  

---

## **🔹 Updated Schema Workflow**  

```
📂 Raw Data (CSV, Excel, JSON, API)  
   ⬇  
🛠 Data Validation & Schema Processing  
   ⬇  
📊 Fact & Dimension Table Structuring  
   ⬇  
📑 Structured Output for BI & Analytics  
```  

---

## **🔹 Multi-Line Header Merging & Variable-Length Row Handling**  

OK **Detects & fixes `NaT` headers by merging multiple rows dynamically**  
OK **Handles cases where row counts differ across header rows**  
OK **Filters out blank header components before merging**  

```python
def merge_multi_row_headers(df, header_rows):
    """Merges multiple header rows into a single header row, handling missing values."""
    max_cols = max(len(df.iloc[row].dropna()) for row in header_rows)  # Get max column count

    merged_headers = []
    for col_idx in range(max_cols):
        merged_parts = []
        for row in header_rows:
            cell_value = str(df.iloc[row, col_idx]).strip() if col_idx < len(df.iloc[row]) else ""
            if cell_value:
                merged_parts.append(cell_value)
        merged_headers.append("_".join(merged_parts) if merged_parts else f"ExtraCol_{col_idx}")

    return merged_headers
```  

---

## **🔹 Alias Dictionary Applied Before Fuzzy Matching**  

OK **Ensures file-specific alias overrides are applied before the global alias dictionary**  
OK **Reduces reliance on fuzzy matching by handling known synonyms first**  

```python
ALIAS_DICT = {
  "cpi_val": "cpi_value",
  "cpi value ": "cpi_value",
  "expenditure_cat": "category"
}

def apply_aliases(col_name, file_name=None):
    """Maps column names using file-specific overrides first, then applies global alias dictionary."""
    if file_name in FILE_SPECIFIC_ALIASES and col_name in FILE_SPECIFIC_ALIASES[file_name]:
        return FILE_SPECIFIC_ALIASES[file_name][col_name]
    return ALIAS_DICT.get(col_name, col_name)
```  

---

## **🔹 Logging Schema Validation Issues with Summarized Results**  

OK **Logs missing and unexpected columns in a structured format**  
OK **Summarizes missing foreign keys instead of logging every row-level issue**  
OK **Supports file-specific schema violation tracking**  

```python
def log_schema_violations(file_name, missing_keys, schema_warnings):
    """Logs missing and unexpected schema violations for better tracking."""
    log_data = {
        "file": file_name,
        "missing_keys_count": len(missing_keys),
        "warnings": schema_warnings
    }
    with open("schema_violations.json", "a") as f:
        json.dump(log_data, f, indent=4)
```  

---

## **🔹 Referential Integrity & Data Type Enforcement Enhancements**  

OK **Prevents invalid foreign keys from entering structured datasets**  
OK **Logs only a count of missing foreign keys instead of row-level details**  

```python
def check_referential_integrity(fact_df, dim_df, key_column):
    """Ensures all fact table keys exist in the dimension table and logs summary results."""
    missing_keys = set(fact_df[key_column]) - set(dim_df[key_column])
    return {"missing_keys_count": len(missing_keys), "status": "All keys validated" if not missing_keys else "Integrity check failed"}
```  

---

## **🔹 Expanded Test Coverage with Synthetic Data for CI/CD Pipelines**  

OK **Validates against a library of known problem cases before production release**  
OK **Ensures every update fixes prior issues without introducing new regressions**  

✔ **Test Cases Covered:**  
  - **Multi-line headers with missing values**  
  - **Files with disclaimers and irrelevant rows**  
  - **Files with alias mismatches & fuzzy borderline cases**  
  - **Files with missing foreign keys & type mismatches**  

✔ **Benefit:** **Prevents regressions and ensures consistent improvements across versions.**  

---

## **🔹 Summary of Version 21.0 Updates**  
OK **Merges multi-line headers dynamically while handling blank cells and different row lengths**  
OK **Ensures alias dictionary is applied before fuzzy matching to improve performance**  
OK **Logs schema validation results in a structured format with summary reports**  
OK **Optimizes referential integrity checks to prevent excessive logging of missing keys**  
OK **Expands synthetic test coverage to validate edge cases in CI/CD pipelines**  

This ensures **greater resilience when dealing with real-world messy datasets.** 🚀

